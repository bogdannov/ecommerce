#name: Deploy to Production
#
#on:
#  push:
#    branches: [main]
#  workflow_dispatch:
#
#jobs:
#  deploy:
#    name: Deploy to Production
#    runs-on: ubuntu-latest
#
#    steps:
#      - name: Checkout
#        uses: actions/checkout@v4
#
#      - name: Deploy via rsync
#        uses: Burnett01/rsync-deployments@8.0.4
#        with:
#          switches: -avzr --delete --exclude-from=.rsyncignore
#          path: ./*
#          remote_path: ${{ secrets.REMOTE_PATH }}
#          remote_host: ${{ secrets.SSH_HOST }}
#          remote_user: ${{ secrets.SSH_USER }}
#          remote_key: ${{ secrets.SSH_PRIVATE_KEY }}
#          remote_port: ${{ secrets.SSH_PORT }}
#
##      - name: Clear LiteSpeed Cache
##        uses: appleboy/ssh-action@v1.0.3
##        with:
##          host: ${{ secrets.SSH_HOST }}
##          username: ${{ secrets.SSH_USER }}
##          key: ${{ secrets.SSH_PRIVATE_KEY }}
##          port: ${{ secrets.SSH_PORT }}
##          script: |
##            cd ${{ secrets.REMOTE_PATH }}
##            wp litespeed-purge all || rm -rf wp-content/cache/* || true
#
#      - name: Notify Success
#        run: echo "✅ Deployed to https://viva-market.pl"
